"use client";
import {FormEvent,useState}from"react";import{configured,resetPassword,signIn}from"../../lib/supabase";import{Logo}from"../../components/Logo";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();if(!configured){setMessage("Add your Supabase keys to .env.local first.");return}try{setMessage("Signing in…");await signIn(email,password);location.href="/admin"}catch(e){setMessage(e instanceof Error?e.message:"Unable to sign in")}}async function reset(){if(!email){setMessage("Enter your email first.");return}try{await resetPassword(email);setMessage("Password reset email sent.")}catch(e){setMessage(e instanceof Error?e.message:"Unable to send reset email")}}return <main className="login"><Logo dark/><form onSubmit={submit}><p>SECURE CONTENT STUDIO</p><h1>Welcome back.</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label><button>Sign in ↗</button><button type="button" className="textButton" onClick={reset}>Forgot password?</button>{message&&<output>{message}</output>}</form></main>}
