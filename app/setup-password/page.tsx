"use client";
import{FormEvent,useEffect,useState}from"react";import{captureInviteSession,getSession,updatePassword}from"../../lib/supabase";import{Logo}from"../../components/Logo";
export default function SetupPassword(){const[ready,setReady]=useState<boolean|null>(null),[message,setMessage]=useState("");useEffect(()=>{const timer=window.setTimeout(()=>setReady(Boolean(captureInviteSession()||getSession())),0);return()=>window.clearTimeout(timer)},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget),password=String(fd.get("password")),confirm=String(fd.get("confirm"));if(password!==confirm){setMessage("Passwords do not match.");return}try{setMessage("Saving…");await updatePassword(password);setMessage("Password created. Opening your content studio…");setTimeout(()=>location.href="/admin",900)}catch(e){setMessage(e instanceof Error?e.message:"Unable to save password")}}return <main className="login"><Logo dark/><form onSubmit={submit}><p>ADMINISTRATOR INVITATION</p><h1>Create your password.</h1>{ready===null?<p>Checking invitation…</p>:ready?<><label>New password<input name="password" type="password" required minLength={10} autoComplete="new-password"/></label><label>Confirm password<input name="confirm" type="password" required minLength={10} autoComplete="new-password"/></label><button>Activate administrator account ↗</button></>:<p>This invitation is missing or has expired. Ask for a new invitation.</p>}{message&&<output>{message}</output>}</form></main>}
